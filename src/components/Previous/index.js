import React from 'react';

function Previous () {

  return (
    <div>
      {/* sidebar with years and main section with photos from the year selected -- photos section may need to be its own react component */}
    </div>
  );
};

export default Previous;
